{
  "name": "velo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "echo 'Static site — no build step needed'"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  }
}
